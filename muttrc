reset all
unhook *
unhdr_order *
ignore *                                     # ignore all headers

set realname = "Philippe Pepiot"
set reverse_name
set from = "phil@philpep.org"

# SSL / TLS
set ssl_starttls=yes
set ssl_force_tls=yes

set smtp_url = smtp://phil@philpep.org@mail.philpep.org:587/
set smtp_pass =
set imap_user         = phil@philpep.org
set imap_pass         =
set folder            = imaps://imaps.philpep.org
set spoolfile         = imaps://imaps.philpep.org
set record="=Sent"
set postponed="=Drafts"
unset imap_passive
set mail_check=60
set header_cache=~/.mutt/.hcache
mailboxes =INBOX
mailboxes =FreeBSD-gnat
mailboxes =FreeBSD-cvs-ports
mailboxes =gandi
mailboxes =ebay
mailboxes =root
mailboxes =Sent
mailboxes =Drafts
mailboxes =Trash
mailboxes =Spam

##################### Mail Stuff #####################

set menu_scroll
set nomove
set copy              = yes                 # copy sent mails
set abort_nosubject   = ask-yes
set abort_unmodified  = ask-yes
set sort              = threads
set edit_headers      = yes
set forward_format    = "Fwd: %s"
set include                                 # include original text in reply
set alias_file        = ~/.mutt/aliases
set sort_alias        = alias
set reverse_alias     = yes
source $alias_file
set nobeep
set strict_threads    = yes
set editor            = vim
set allow_8bit
set locale="en_US.UTF-8"
set send_charset="utf-8"
set config_charset="utf-8"
reset charset

set implicit_autoview
auto_view text/html  application/x-pgp-message
alternative_order text/plain text/enriched text text/html  # Show the "text/plain" part *before* the "text/html" part
#set mailcap_path="~/.mailcap"
#set mailcap_sanitize=yes

# GPG
#set crypt_autosign
#set crypt_replyencrypt
#set crypt_replysign
#set pgp_verify_sig=yes
#source ~/.mutt/gpg.rc

#my_hdr X-PGP-Key: http://files.kaworu.ch/alexandre.perrin.asc
my_hdr Content-Type: text/plain\; charset=utf-8


set smileys="(>From)|(:[-^]?[][)(><}{|/DP])"
set pager_context=3 # number of lines that overlap on paging
set pager_index_lines=12
set pager_stop # "next_page" won't jump to next message at end of messages

#mailboxes =INBOX
#mailboxes =Spam
#mailboxes =Sent
#subscribe FreeBSD-current awesome-devel ruby-fr yaml-core
#macro index G "!fetchmail\r"
#macro pager G "!fetchmail\r"
macro index \Cr \
    "<tag-pattern>~N\n<tag-prefix><clear-flag>N" \
    "Mark all new messages as read"
bind pager \CD half-down
bind pager \CU half-up
bind index \CD half-down
bind index \CU half-up
bind pager j next-line
bind pager k previous-line
bind pager \Cj next-undeleted
bind pager \Ck previous-undeleted

unignore  From: To Cc Reply-To Mail-Followup-To Subject User-Agent X-Mailer Date X-Spam-Level:
hdr_order Date From: To Cc Reply-To Mail-Followup-To  User-Agent X-Mailer X-Spam-Level: Subject

#set header_cache=~/.mutt/hcache

##################### Color Stuff #####################


color attachment brightgreen default
color error brightwhite red   # errors yell at you in red
color hdrdefault blue default   # headers
color indicator black white # currently selected message
color markers blue default  # the + for wrapped pager lines
color message brightblue default  # informational messages, not mail
color normal white default    # plain text
color search black brightyellow # hilite search patterns in the pager
color signature red default    # signature (after "-- ") is red
color status white blue # status bar
color tilde yellow default  # ~'s after message body
color tree red default    # thread tree in index menu is magenta
color underline green default
color header brightwhite default "^(From|Subject):" # Important headers
color signature brightred default
color body black red "^gpg: Bad signature from .*"
color body black green "^gpg: Good signature from .*"
color body black yellow "^gpg: Can't check signature:.*"
color body brightyellow default "[:;]+[-^o]?[])>}D]+"
color body brightwhite default "(ftps?|https?)://[^ ]+"  # picks up URLs
color body brightwhite default "www\.[^ ]+"
color body brightwhite default "[-a-z_0-9.]+@[-a-z_0-9.]+"

color index brightyellow default ~N      # New
color index yellow default ~O              # Old
color index magenta default ~F
color index yellow default ~T
color index red default ~D

color quoted cyan default
color quoted1 magenta default
color quoted2 green default
color quoted3 yellow default
color signature brightblue default


##################### Sidebar Stuff #####################


set sidebar_width=24
set sidebar_visible=yes

#color sidebar_new yellow default

bind index \CP sidebar-prev
bind index \CN sidebar-next
bind index \CO sidebar-open
bind pager \CP sidebar-prev
bind pager \CN sidebar-next
bind pager \CO sidebar-open
